// setTimeout → Ek function ko specified delay ke baad sirf ek baar execute karta hai.

// setInterval → Ek function ko fixed interval par baar-baar execute karta hai.

// Hoisting → JavaScript me variables (var) aur functions declaration execution se pehle memory me upar move ho jate hain.

// Temporal Dead Zone (TDZ) → let aur const variable declare hone se pehle access karne par ReferenceError aata hai.

// First, Second, Third Largest → Array ko descending sort karke index 0, 1, 2 access kar lo.

// Nested Object me Total IDs Count → Recursive function bana kar har item traverse karo aur count increase karo.

// Closure → Jab inner function outer function ke variables ko yaad rakhta hai even after outer function execution.

// setTimeout me Closure Concept → Loop ke andar let use karo ya IIFE use karo taaki har iteration ka alag scope bane.

// useTransition Hook → React ka hook jo low priority state updates ko background me run karta hai.

// Two String Alternate Merge → Loop chalao aur har index par str1[i] + str2[i] concat karo.

// 1 Lakh Data UI me Display → Pagination, Infinite Scroll, Virtualization (react-window), Lazy Loading use karo.

// Missing Number (1–100) → Formula use karo (n*(n+1))/2 minus array sum.

// Debouncing → Function tab run hota hai jab user action stop kar deta hai kuch time ke liye.

// Throttling → Function fixed time interval par hi run hota hai.

// useDispatch → Redux store ko action bhejne ke liye use hota hai.

// useSelector → Redux store se state lene ke liye use hota hai.

// Redux Flow → Dispatch → Action → Reducer → Store Update → UI Re-render.