// Avoid using index as a key in React; opt for stable and unique identifiers for better rendering and updates.





// If we add a new object in our data then our index will get changed so that's why we can't take index as a key 
// because key should be unique.