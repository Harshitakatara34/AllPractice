// Sets are JavaScript data structures that store unique values. 
// They are similar to arrays but only hold distinct elements.




// const mySet = new Set();
// mySet.add(1);
// mySet.add(2);
// mySet.add(2); // Duplicate is not added
// console.log(mySet); // Output: Set { 1, 2 }



// In this example, the Set stores unique values, and duplicate values (2 in this case) 
// are automatically removed, resulting in a set with unique elements.