// Impure Function: Can produce different outputs for the same input or has side effects that affect external state.

// Impure function
// let total = 0;

// function addToTotal(value) {
//   total += value;
// }

// addToTotal(5);
// console.log(total); // Output: 5

// addToTotal(3);
// console.log(total); // Output: 8 (Different output for the same function call)
