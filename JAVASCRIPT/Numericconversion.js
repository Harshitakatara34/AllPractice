

// Edge Cases for Numeric Conversion

// `undefined` becomes `NaN`

// `null` becomes `0`

// `true` becomes `1`

// `false` becomes `0`

//  `123`  becomes `123`




// let tempo
// console.log(Number(tempo))   NAN